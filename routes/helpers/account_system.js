module.exports.checkAdmin = function(user, res) {
    console.log("current user: " + user);
    /*
    if (user == undefined)
        res.redirect("/");
    if (!user.admin)
        res.redirect("/");
        */
}
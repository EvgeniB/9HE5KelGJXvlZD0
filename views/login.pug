extends layout

block content

    form(method="POST" action="/")
        p Username:
        input(type="text")
        p Password:
        input(type="text")
        input(type="submit" value="Login")

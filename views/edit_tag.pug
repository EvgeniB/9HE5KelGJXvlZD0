extends layout

block content

    form(method='POST')
        br
        p Name:
        input(type='text', name='name', value=tag.Name)
        br
        input(type='submit', value='Save')
        a(href='/edit_tags')
            input(type='button', value='Cancel')
extends layout

block content

    form(method='POST')
        br
        p Name:
        input(type='text', name='name', value=event_type.Name)
        br
        input(type='submit', value='Save')
        a(href='/edit_event_types')
            input(type='button', value='Cancel')